IStack main() {
  IStack x = new CountedListStack : IStack();
  IStack y = null;

  x.push(new Character : ICharacter(value = '5'));
  x.push(new Integer   : IInteger(value = 3));

  y = x.copy();

  while(!y.empty()) {
    Any curr = y.pop();

    if curr isa ICharacter {
      println("Got a character");
    } else {
      if curr isa IInteger {
        println("Got an integer");
      } else {
        println("Got something else");
      };
    };
  };

  x;
}
