(module info (lib "infotab.ss" "setup")
  (define name "Python")

  (define tool-icons '(("the_foot.gif" "icons")))
  ;;(define tool-names (list "Python"))
  (define tools (list (list "tool.ss"))))
