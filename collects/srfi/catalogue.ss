;; WARNING: This file was generated automatically by add-package (SRFI-0)
;;  DO NOT hand edit this file! (unless, of course, you do know what you are doing)
(module catalogue mzscheme
  (provide cond-expand)
  (define-syntax
   cond-expand
   (syntax-rules (srfi-25
                  srfi-7
                  srfi-28
                  srfi-26
                  srfi-23
                  srfi-19
                  srfi-18
                  srfi-17
                  srfi-16
                  srfi-14
                  srfi-11
                  srfi-9
                  srfi-8
                  srfi-5
                  srfi-2
                  srfi-1
                  srfi-6
                  srfi-13
                  and
                  or
                  not
                  else
                  srfi-0)
     ((cond-expand (srfi-25 body ...) more-clauses ...)
      (begin (require (lib "25.ss" "srfi")) body ...))
     ((cond-expand (srfi-7 body ...) more-clauses ...)
      (begin (require (lib "7.ss" "srfi")) body ...))
     ((cond-expand (srfi-28 body ...) more-clauses ...)
      (begin (void) body ...))
     ((cond-expand (srfi-26 body ...) more-clauses ...)
      (begin (require (lib "26.ss" "srfi")) body ...))
     ((cond-expand (srfi-23 body ...) more-clauses ...)
      (begin (void) body ...))
     ((cond-expand (srfi-19 body ...) more-clauses ...)
      (begin (require (lib "19.ss" "srfi")) body ...))
     ((cond-expand (srfi-18 body ...) more-clauses ...)
      (begin (void) body ...))
     ((cond-expand (srfi-17 body ...) more-clauses ...)
      (begin (require (lib "17.ss" "srfi")) body ...))
     ((cond-expand (srfi-16 body ...) more-clauses ...)
      (begin (void) body ...))
     ((cond-expand (srfi-14 body ...) more-clauses ...)
      (begin (require (lib "14.ss" "srfi")) body ...))
     ((cond-expand (srfi-11 body ...) more-clauses ...)
      (begin (void) body ...))
     ((cond-expand (srfi-9 body ...) more-clauses ...)
      (begin (require (lib "9.ss" "srfi")) body ...))
     ((cond-expand (srfi-8 body ...) more-clauses ...)
      (begin (require (lib "8.ss" "srfi")) body ...))
     ((cond-expand (srfi-5 body ...) more-clauses ...)
      (begin (require (lib "5.ss" "srfi")) body ...))
     ((cond-expand (srfi-2 body ...) more-clauses ...)
      (begin (require (lib "2.ss" "srfi")) body ...))
     ((cond-expand (srfi-1 body ...) more-clauses ...)
      (begin (require (lib "1.ss" "srfi")) body ...))
     ((cond-expand (srfi-6 body ...) more-clauses ...) (begin (void) body ...))
     ((cond-expand (srfi-13 body ...) more-clauses ...)
      (begin (require (lib "13.ss" "srfi")) body ...))
     ((cond-expand) (syntax-error "Unfulfilled cond-expand"))
     ((cond-expand (else body ...)) (begin body ...))
     ((cond-expand ((and) body ...) more-clauses ...) (begin body ...))
     ((cond-expand ((and req1 req2 ...) body ...) more-clauses ...)
      (cond-expand
        (req1 (cond-expand ((and req2 ...) body ...) more-clauses ...))
        more-clauses
        ...))
     ((cond-expand ((or) body ...) more-clauses ...)
      (cond-expand more-clauses ...))
     ((cond-expand ((or req1 req2 ...) body ...) more-clauses ...)
      (cond-expand
        (req1 (begin body ...))
        (else (cond-expand ((or req2 ...) body ...) more-clauses ...))))
     ((cond-expand ((not req) body ...) more-clauses ...)
      (cond-expand (req (cond-expand more-clauses ...)) (else body ...)))
     ((cond-expand (srfi-0 body ...) more-clauses ...) (begin body ...))
     ((cond-expand (feature-id body ...) more-clauses ...)
      (cond-expand more-clauses ...)))))
