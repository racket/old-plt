- Dim (or vanish) the Step button when not in appropriate language level
- Forward structure declarations (?)
- Definitions like (define g (if #t + f))
- retention of unit vars when the only marks are from distant closures
