
@BOOLEAN Bool
@CLASSSUFFIX "%"
@IDFIELD __type

@MACRO NOZERO[p] = if (!x<p>) x<p> = -1;
@MACRO CHECKOK[p.who] = if (x<p> && !x<p>->Ok()) scheme_arg_mismatch(<who>, "bad bitmap: ", p[<p>]);
@MACRO CHECKDCOK[p.who] = if (x<p> && !x<p>->Ok()) scheme_arg_mismatch(<who>, "bad device context: ", p[<p>]);
@MACRO CHECKVOIDABLEOK[p] = if (x<p> && !x<p>->Ok()) return scheme_void;
