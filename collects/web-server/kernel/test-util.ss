(module test-util mzscheme
  (require (lib "url.ss" "net")
           "test-harness.ss"
           "util.ss")

  (test "test-util doesn't do anything anymore. Need to refactor util.ss"
        (lambda () #f))

  )
